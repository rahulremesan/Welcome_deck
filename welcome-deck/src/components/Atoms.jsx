import { atom } from 'recoil'

export const employeeDetailsState = atom({
    key:'employeeDetailsState',
    default: null,
});
export const employeeImageDataState = atom({
    key: 'employeeImageDataState',
    default: null,
});
